# Copy to .env. One key is enough if both roles use the same provider.
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# Optional model overrides (see agent/llm_client.py for the defaults).
# CHAT_MODEL=gemini-flash-lite-latest
# EVOLUTION_MODEL=gemini-flash-latest

# Optional run settings; every one has a default (see README).
# CE_LLM_RPM=8
# CE_SANDBOX_REPEATS=3
# CE_SANDBOX_CONCURRENCY=1
